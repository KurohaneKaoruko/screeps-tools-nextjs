import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: '市场查询 | Screeps Tools',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
