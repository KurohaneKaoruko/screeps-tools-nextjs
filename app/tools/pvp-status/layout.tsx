import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'PvP 战争情况 | Screeps Tools',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
