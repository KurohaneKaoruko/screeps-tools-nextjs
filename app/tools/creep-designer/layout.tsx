import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Creep 设计器 | Screeps Tools',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
