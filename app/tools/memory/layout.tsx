import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Memory 查看器 | Screeps Tools',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
