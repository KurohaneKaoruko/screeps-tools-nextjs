import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Nuke 打击情况 | Screeps Tools',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
